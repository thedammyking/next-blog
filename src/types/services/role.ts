export type CreateRoleData = {
  label: string;
  value: string;
};

export type CreatePostData = {
  title: string;
  excerpt: string;
  featuredImage: string;
  body: string;
  authorId: string;
};
